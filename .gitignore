node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Derived build output — regenerated by `npm run build` (deploy-universal.js).
# Deployments build fresh (Vercel -> dist/public, Render -> server), so these
# mirror folders should not be tracked.
/build
/public

# Secrets and local env — never commit these
.env
.env.local
.env.*.local
.local